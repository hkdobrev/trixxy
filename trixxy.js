(function( window, undefined ) {

	function Stack( cards ) {
		this.cards = cards;
	}

	Stack.isOK = function 

	function Card( map, rotation, flip ) {

		function processMap ( map ) {
			return map;
		}


		this.map = processMap( map );
		this.rotation = rotation;
		this.flip = flip;
		this.rows = Card.ROWS;
		this.COLUMNS = Card.COLUMNS;
	}

	function Face()

	/**
	 * Card constants
	 */

	Card.COLORS = ['yellow', 'red', 'green', 'purple'];
	Card.ROWS = 4;
	Card.COLUMNS = 4;

}( window ));